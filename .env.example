# Invoice OCR Service Environment Variables
# For Railway deployment: Set these in Railway Dashboard

# ===========================================
# REQUIRED - Server Configuration
# ===========================================
# Railway automatically sets PORT - no need to set this manually on Railway
PORT=8080
HOST=0.0.0.0

# ===========================================
# REQUIRED - AI Provider (choose one)
# ===========================================
# Google Gemini Configuration (Recommended for Railway - free tier available)
GEMINI_API_KEY=AIza-your-key-here

# OpenAI Configuration (Alternative)
OPENAI_API_KEY=sk-your-key-here

# Ollama Configuration (Local only - not recommended for Railway)
OLLAMA_BASE_URL=http://localhost:11434

# ===========================================
# OPTIONAL - OCR Configuration
# ===========================================
# Tesseract language codes (comma-separated)
# Examples: eng (English), spa (Spanish), spa+eng (both)
TESSERACT_LANG=spa+eng

# ===========================================
# OPTIONAL - Upload & Processing Limits
# ===========================================
# Maximum file size in bytes (10MB default)
MAX_FILE_SIZE=10485760

# Processing timeout in seconds
PROCESSING_TIMEOUT=30

# ===========================================
# OPTIONAL - AI Provider Selection
# ===========================================
# Default AI provider: openai, gemini, or ollama
AI_DEFAULT_PROVIDER=gemini

# Specific model to use (provider-dependent)
# OpenAI: gpt-4, gpt-4-vision-preview, gpt-3.5-turbo
# Gemini: gemini-pro, gemini-pro-vision
# Ollama: mistral, llama2, phi
GEMINI_MODEL=gemini-pro
OPENAI_MODEL=gpt-4

# ===========================================
# OPTIONAL - Feature Flags
# ===========================================
# Enable vision model processing (skip OCR, send image directly to AI)
ENABLE_VISION_MODEL=false

# Enable debug logging
ENABLE_DEBUG_MODE=false

# ===========================================
# OPTIONAL - Railway-Specific (auto-set by Railway)
# ===========================================
# Railway environment name (production, staging, etc.)
RAILWAY_ENVIRONMENT=production

# Railway service name
RAILWAY_SERVICE_NAME=invoice-ocr-service
